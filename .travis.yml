python:
    - "2.6"
    - "2.7"
    - "pypy"
before_install: sudo apt-get install libssl-dev -y
install: sudo pip install -r requirements.txt --use-mirrors
script: find . -name '*.py' |xargs pyflakes
script: trial tests.py plugins